package com.example.junit;

public class Calculator {

	
	/*
	 * Maven:
	 * - a build automation tool used primarily
	 * for Java project.
	 * - project management tool that is based
	 * on POM.
	 * - dependency manager (handles our dependencies)
	 * 
	 * Dependencies:
	 * - any libraries (jar files) that are needed
	 * for our program to run
	 */
	
	// method to add two numbers
	// method to subtract two numbers
	// method to multiply two numbers
	// method to divide two numbers
	// method to check whether two numbers are equal
	// optional: calculate the square root
	// optional: exponents
	// optional: find the remainder of two numbers (modular arithmetic)
	
	public boolean areTwoIntegersEqual(int x, int y) {
		if (x == y)
			return true;
		else {
			return false;
		}
	}
}
